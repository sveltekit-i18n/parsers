name: Curly parser tests

on:
  push:
    paths:
      - parser-curly/**
      - .github/workflows/tests-parser-curly.yml
      - .github/workflows/tests.yml
  workflow_dispatch:

jobs:
  tests:
    uses: ./.github/workflows/tests.yml
    with:
      package: parser-curly
