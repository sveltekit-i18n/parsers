# 3.0.0
Initial release. `@sveltekit-i18n/parser-mf2` brings [Unicode MessageFormat 2](https://unicode.org/reports/tr35/tr35-messageFormat.html) to [`@sveltekit-i18n/base`](https://github.com/sveltekit-i18n/base) v3:
* Every message is compiled and formatted by [`messageformat`](https://github.com/messageformat/messageformat) v4, the format's reference implementation and the package's only runtime dependency. Compiled messages are cached per parser instance (least-recently-used, up to 10,000 entries keyed by locale and message), and base's parser contract checks (the repository's `contract/` set) run beside the package's own tests: what a message MEANS is the specification's to certify, that the parser can be called the way base calls it is base's.
* `parser(options)` takes the engine's options (`bidiIsolation`, `dir`, `localeMatcher`, `functions`) and `onReport`, and returns the parser the base library expects; `parse(value, [payload], locale, key)` maps the library's `t(key, payload?)` onto the format's inputs. The functions the specification classifies as DRAFT (`:date`, `:datetime`, `:time`, `:currency`, `:percent`, `:unit`) are registered by default, so dates and money format out of the box as they do with the other official parsers; `functions` spreads over them.
* The parser fails soft. A message that cannot be compiled is returned raw and reported as `failed-message`; an expression that cannot be resolved renders as the format's own fallback for it (`{$name}`) while the rest of the message renders, reported as `fallback-value`. A key naming no message yields the empty string: what a missing translation renders as is [`fallbackValue`](https://github.com/sveltekit-i18n/base/blob/master/docs/README.md#fallbackvalue), which base answers before this parser is called.
* `onReport` is required, `null` included: the parser writes to no channel of its own - the engine's own process warnings are routed through it - so where a report goes is stated by whoever builds the parser. A report carries `code`, the `key` and `locale` of the call, a one-sentence `message`, and the `error` the engine threw or handed back where there was one; a report channel that throws is contained like any other failure.
* `extractParamsFactory` reads the parameters a message names, as a named export beside the default one. Each parameter says the payload key it is named by, what the functions annotating it narrow it to, the values a `:string` selector or a numeric selector's exact keys name explicitly, and the variants it lives under. It is the build-time half of the base parser contract, which is why it is a named export rather than a member of the parser object: a message scanner is of no use while rendering, and the package declares `sideEffects: false` so a bundle that never reaches it drops it.
* `Config` and `Parser` are the exported types.
* ESM only; requires Node.js 22.12, Bun 1.2 or Deno 2, or newer; `@sveltekit-i18n/base` v3 is a peer dependency.
